# Fading Furigana Development Plan

This document breaks development into small, completable tasks. Update the status whenever a task is finished.

Status values:

```text
done
in_progress
pending
blocked
```

## Current Status Summary

```text
Current branch: dev
Current phase: Extension/App decoupling and ingest contract
Next recommended task: T077 pairing UX done and loopback wire smoke-tested; next is the manual Chrome-browser GUI run (load unpacked extension, pair, browse) and routing the content-script flush through the background worker. T076 still needs a real Safari page round trip
```

## Tasks

| ID | Status | Task | Deliverable | Verification |
| --- | --- | --- | --- | --- |
| T001 | done | Build initial annotation prototype | Demo annotates sample Japanese text with ruby and tooltip actions | Manual demo test |
| T002 | done | Add demo toggle | Demo can hide and show annotations | Manual demo test |
| T003 | done | Add katakana loanword sample | `サーバー` displays `server` as source-form hint | Manual demo test |
| T004 | done | Deploy demo through Cloudflare Workers | `/test-page` serves demo and JS/CSS assets | `wrangler deploy` / online smoke test |
| T005 | done | Add GitHub Actions Cloudflare deployment | Push to `dev` deploys with Wrangler action | GitHub Actions run |
| T006 | done | Fix Cloudflare assets upload | Exclude generated dependencies from Worker assets | `npx wrangler@latest deploy --dry-run` |
| T007 | done | Upgrade requirements document | Requirements reflect adaptive lexical annotation, multilingual meanings, exposure statistics, privacy | Document review |
| T008 | done | Add AppState v1 and storage adapter | `AppState`, migration helpers, `LocalStorageAdapter` | Manual demo test, `node --check` |
| T009 | done | Extract repository layer | Lexical item, user lexical state, and exposure repositories | `node --check`, dry-run |
| T010 | done | Add automated state/repository tests | Lightweight Node tests for state, migration, storage, repositories | `npm run check`, `npm test` |
| T011 | done | Add development design and task plan docs | `docs/DEVELOPMENT_DESIGN.md` and this plan | Document review |
| T012 | done | Extract annotation decision module | Move `shouldAnnotate` and related settings logic out of content script | `npm run check`, `npm test`, HTTP script load check, Wrangler dry-run; browser automation unavailable this run |
| T013 | done | Extract DOM annotation engine module | Move DOM scanning, ruby insertion, restore, mutation handling out of content script | `npm run check`, `npm test`, HTTP script load check, Wrangler dry-run |
| T014 | done | Extract tooltip module | Move tooltip rendering and actions out of content script | `npm run check`, `npm test`, HTTP script load check, Wrangler dry-run |
| T015 | done | Add DOM annotation tests | Test skip tags, duplicate prevention, restore, and annotation output | `npm run check`, `npm test` |
| T016 | done | Add exposure write batching | Avoid saving state once per token on large pages | `npm run check`, `npm test`, HTTP script load check, Wrangler dry-run |
| T017 | done | Add ChromeStorageAdapter | Store AppState in `chrome.storage.local` | `npm run check`, `npm test`, HTTP script load check, Wrangler dry-run |
| T018 | done | Add extension settings UI | Toggle annotation, exposure tracking, URL privacy mode | `npm run check`, `npm test`, HTTP popup load check, Wrangler dry-run |
| T019 | done | Add vocabulary/frequency debug UI | Show today's frequent items and this week's recurring items | `npm run check`, `npm test`, HTTP popup load check, Wrangler dry-run |
| T020 | done | Add real dictionary/tokenizer provider design spike | Choose local dictionary/tokenizer/API strategy | `docs/DICTIONARY_TOKENIZER_SPIKE.md`, source review |
| T021 | done | Add local dictionary provider prototype | Replace sample-only dictionary for a small real subset | `npm run check`, `npm test`, HTTP script load check, Wrangler dry-run |
| T022 | done | Add Safari storage bridge design | Define background/native/App Group message path | `docs/SAFARI_STORAGE_BRIDGE_DESIGN.md` |
| T023 | done | Add Safari Web Extension packaging | Xcode app container and Safari extension target | `npm run package:safari`, `xcrun safari-web-extension-packager`, `xcodebuild -list`, macOS no-sign build |
| T024 | done | Extract word repository service | Move annotation data coordination out of content script | `npm run check`, `npm test`, HTTP script load check, `npm run package:safari` |
| T025 | done | Add review and frequency-based learning screens | Mac app review session (sheet over shared AppState): due queue orders lapsed words, never-reviewed saves, then overdue reviews; Forgot/Got It/Easy grading with ladder SRS (1/3/7/14/30/60/120 days; graduation to `known` + hidden annotation at 6-streak; forgot resets to `lapsed` and re-queues); every grade appends an append-only `reviewLogs` entry with a client-generated ID per the sync contract; dashboard adds Due Reviews metric, Start Review button, and frequency-based "Suggested" tab (frequently seen but unlearned words); Learning list/metric now include the `reviewing` stage | `npm run check`, `npm test`, `npm run build:safari:mac` (installed app verified to contain the review code); manual review-session walkthrough pending |
| T026 | done | Add extension interface language setting | Store UI language in AppState and switch popup text between Chinese and English | `npm run check`, `npm test`, `npm run package:safari` |
| T027 | done | Integrate packaged JMdict common data | Load 5,000 real JMdict entries locally through DictionaryProvider | `npm run check`, `npm test`, `npm run package:safari` |
| T028 | done | Skip annotation on Chinese pages | `src/core/pageLanguage.js` gates the annotation engine via declared `lang` plus kana/han heuristic | `npm run check`, `npm test`, `npm run package:safari` |
| T029 | done | Fix settings propagation across tabs | Content tabs preserve stored settings on persist, listen to `storage.onChanged`; popup re-reads state before saving | `npm run check`, `npm test` (lost-update regression test) |
| T030 | done | Tokenizer phase A: Intl.Segmenter boundary guard | Dictionary matches constrained to ICU word boundaries; digit+counter readings (年/月/時/円/回/個/歳/台/番/人), irregular counters skipped, date-counter compound guard | `npm run check`, `npm test` (gold-sentence tests), `npm run package:safari` |
| T031 | done | Tokenizer phase B: kuromoji.js in background worker | MV3 service worker hosts kuromoji (vendored build + IPADIC dict); content scripts batch-tokenize via messaging with local-analyzer fallback; counter safety net applied to IPADIC output | `npm run check`, `npm test` (mapper unit + real-kuromoji integration tests), worker simulation, `npm run package:safari`; manual extension test pending |
| T032 | done | Tiered dictionary coverage expansion | JMdict generator supports `--tier=common|priority|full`; packaged eager priority tier now contains all priority-marked JMdict surfaces (43,807 entries in current source) with compact output and tier metadata; full tier remains reserved for future lazy/background lookup | `npm run check`, `npm test`, `node scripts/buildJmdictCommonData.js --tier=priority /private/tmp/JMdict_e.gz` |
| T033 | done | Level-based filtering and per-word actions | `annotation.userLevel` popup setting; JMdict-priority difficulty heuristic; tooltip actions for "forgot this word" and "always annotate"; pinned words override level filtering | `npm run check`, `npm test`; right-click context menu deferred |
| T034 | in_progress | Height-constrained layout fallback | Default `tap_only` rendering inside line-clamped/fixed-height containers; popup display style selector supports auto, ruby, and compact tap hints; per-site "expand container" mode still deferred | `npm run check`, `npm test`; Google results manual test pending |
| T035 | done | Fix annotation performance on large/dynamic pages | Incremental annotation: processed-node tracking (WeakSet), mutation-scoped scanning instead of full-page rescans, chunked tokenizer batches, single settings-refresh path (storage.onChanged only), exposure persist batched at 2s; runaway-loop fuses: 50k-node per-page budget with self-suspend, one exposure per word per page | `npm run check`, `npm test` (incremental + budget/dedup regression tests), Safari manual test after memory blowup report |
| T036 | done | Viewport-lazy annotation | Text nodes wait in an IntersectionObserver queue (600px lookahead) and are only tokenized when their parent element scrolls near the viewport; exposure stats now mean "actually displayed on screen"; tokenization skipped entirely while annotation is off | `npm run check`, `npm test` (viewport deferral test), Safari manual test |
| T037 | done | Stop annotation flicker on loading pages | Stability gate: defer annotation until the DOM has been quiet 600ms (max 6s after boot); per-element churn backoff: elements re-annotated >4 times cool down 5s, >12 times are abandoned | `npm run check`, `npm test` (settle + churn backoff tests), Safari manual test on long loading pages |
| T038 | done | Fix storage-driven refresh loop (flicker on static pages) | `WordRepositoryService.load()` no longer writes storage back (broke the persist→onChanged→load→persist cycle); onChanged settings comparison normalizes both sides; reloads are serialized with an in-flight guard | `npm run check`, `npm test` (read-only load test), Safari manual test on Wikipedia search page |
| T039 | done | Fix storage quota exhaustion | `unlimitedStorage` permission; exposure-only words store no meanings; exposure summaries store no page titles, capped pages/surface forms; `compactState` prunes expired summaries and slims legacy bloat on every persist; quota failures warn once instead of spamming | `npm run check`, `npm test` (compaction/diet tests), Safari console clean after browsing |
| T040 | done | Targeted word refresh on tooltip actions | Save/known/ignore re-evaluate only that word's rubies in place (`engine.refreshWord`); no full restore, no re-tokenization, no page-wide flash; ruby dataset carries source confidence for faithful re-evaluation | `npm run check`, `npm test` (targeted refresh tests), Safari manual test |
| T041 | done | Document local-first and paid sync product architecture | Earlier requirements defined the local-first client model, paid cloud sync, Safari macOS app as one client, and future Chrome/macOS/iOS/Android/Windows roles. Superseded commercially by T058: one-month trial, Basic one-time purchase per platform, Pro subscription for sync and advanced intelligence. | Document review |
| T042 | done | Design sync protocol and multi-client architecture | `docs/SYNC_AND_CLIENTS_DESIGN.md`: op-log sync with per-domain merge rules, golden test vectors as cross-language contract, unified entitlements over IAP/Play/Stripe, App Group topology on Apple platforms, CF Workers + D1 server, 4-phase rollout | Document review |
| T043 | done | Phase 0 sync schema prep | Added `packages/core-schema` JSON Schema, merge rules, and a golden migration vector test; AppState migration now fills stable `metadata.deviceId` plus per-record `id`/`updatedAt`/`deviceId` for syncable domains while preserving unknown fields; JS repositories and Swift Mac actions stamp record metadata at mutation points. Full sync outbox/server ops remain part of the later Pro sync implementation. | `npm run check`, `npm test`, `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-t043 npm run build:safari:mac` |
| T044 | done | Mac app MVP learning dashboard | macOS container app now renders a native dashboard over AppState JSON: extension status, vocabulary/today/learning/saved metrics, Today/7 Days/Learning/Saved/Known/Ignored lists, and word actions for save/known/forgot/ignore/always show | `npm run check`, `xcodebuild -project "apps/apple/Fading Furigana/Fading Furigana.xcodeproj" -scheme "Fading Furigana (macOS)" -configuration Debug -derivedDataPath /tmp/fading-furigana-mac-mvp-build CODE_SIGN_IDENTITY=- build` |
| T045 | done | Safari native AppState bridge | Safari content/popup uses `SafariNativeStorageAdapter`; native handler reads/writes the shared App Group AppState JSON; Mac app reads/writes the same path; macOS targets include App Group entitlements. Signed local build validation confirmed the core loop: Safari page save -> App Group AppState -> Mac app Refresh shows the word -> Mac app state actions write back. | `npm run check`, `npm test`, `npm run package:safari`, `FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-local-ci npm run ci:local`, signed local Safari/Mac app walkthrough |
| T046 | done | Mac app dashboard UI redesign | HIG-style layout: source-list sidebar (sections with SF Symbol icons + counts) over NSSplitView, content column capped at 860pt, NSTableView word list with status badges (New/Learning/Reviewing/Lapsed/Known/Ignored) and per-row contextual "⋯" action menus, extension status as colored dot + text, store path/version demoted to footer; macOS deployment target raised 10.14 → 13.0 for NSTableView styles and SF Symbols | `npm run check`, `npm test`, `npm run build:safari:mac`; manual visual check pending |
| T047 | done | Mac app native settings sheet | Gear button opens an in-app settings sheet instead of jumping to Safari extension preferences: edits the shared AppState `settings` (annotation enabled/mode/level/display style/hide-known, exposure tracking enabled/URL privacy/retention days, popup language) with write-through saves that preserve unknown settings keys; "Open Safari Extension Settings…" lives in the sheet, while direct data-file reveal/export is intentionally not exposed as a user feature. | `npm run check`, `npm test`, `npm run build:safari:mac`; manual settings round-trip (app edit → Safari popup reflects) pending |
| T048 | done | Mac app interface localization | New `Localization.swift` (`L` helper, English strings as keys with zhHans table); app UI language follows shared `settings.display.interfaceLanguage` — the same switch as the extension popup; dashboard re-localizes on every load (static labels/tooltips via registry), review sheet localized at build time, settings sheet rebuilds in place when the language popup changes; "Popup language" renamed "Interface language" | `npm run check`, `npm test`, `npm run build:safari:mac`; manual language-switch walkthrough pending |
| T049 | done | Mac app review context sentences | Review card shows the source sentence the word was seen in: `AppStateSnapshot` indexes the latest `sourceOccurrences` per item, `WordRow` carries `exampleSentence`/`exampleSource` (degenerate sentence==surface filtered out). Card front shows the sentence with the target surface bolded/accent-highlighted (no kana, so no answer leak); reveal adds reading + meaning + `From: title · domain`. Saved/learning rows in the dashboard list gain a one-line context preview; row height 56→64 | `npm run check`, `npm test`, `npm run build:safari:mac`; computer-use walkthrough confirmed front sentence highlight, reveal source line, and Saved-list preview |
| T050 | done | Storage / bridge diagnostics | `SafariNativeStorageAdapter` records the last transport (`native`/`fallback`), `lastSuccessAt`, `lastError` and exposes `getStorageStatus()`; Chrome/Local adapters return `{transport:"local"}`. Popup gains a diagnostics panel: storage mode (Native ✓ / Local fallback ⚠ / Local) + last-updated relative time, i18n zh/en. Mac app adds `AppStateStore.isUsingAppGroup` and upgrades the footer to a diagnostics row (version · `Storage: Shared container/Local fallback` with full path in tooltip · `Updated <relative>`), localized | `npm run check`, `npm test` (new adapter-status unit tests), `npm run build:safari:mac`; computer-use confirmed Mac footer shows storage location + last-updated; popup panel covered by unit tests + manual check |
| T051 | done | Mac app first-run onboarding | When the shared store has no words (or the extension is reported disabled), the dashboard content area shows a 3-step onboarding card instead of the empty list: (1) enable the Safari extension — live ✓/status text + "Open Safari Extension Settings…", (2) browse Japanese pages, (3) come back and Refresh. Reuses `SFSafariExtensionManager` state + `openSafariExtensionPreferences`; "no data" is the primary trigger so a transient extension-status read error doesn't nag returning users; hidden once data exists; localized zh/en | `npm run check`, `npm test`, `npm run build:safari:mac`; computer-use confirmed the 3-step card renders localized with step-1 ✓ Enabled, and is correctly hidden when data is present |
| T052 | done | Mac app MVP information architecture | Dashboard now separates the product loop into `Today` (review queue, daily stats, suggested words), `Library` (searchable/filterable managed vocabulary), and `Discover` (frequent new words by today/7 days/all time). Adds localized zh/en labels for the new sections and keeps row-level Save/Known/Forgot/Ignore/Always Show actions. | `npm run check`, `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-review-build npm run build:safari:mac`; manual visual pass pending |
| T053 | pending | MVP beta release hardening | Complete `docs/MVP_RELEASE_CHECKLIST.md`: signed release build, clean install/upgrade smoke tests, Safari settings walkthrough, real-page annotation smoke tests, privacy copy, and local recovery guardrails before distributing to external testers. User-facing portable export/import is intentionally out of scope because cross-device migration belongs to paid sync. | Checklist review plus signed local release candidate walkthrough |
| T054 | done | Mac app word details and collapsible navigation | Word rows now open a native detail sheet from double-click or the info button, showing reading, meaning, status, seen count, next review, example/source, and direct Save/Known/Forgot/Ignore/Always Show actions. The left navigation with Today/Library/Discover can collapse from the header to give the word content more space on smaller windows. | `npm run check`, `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-detail-build npm run build:safari:mac`; manual sheet interaction pending |
| T055 | done | Mac app automatic AppState refresh | The Mac app now polls the shared AppState file signature every 2 seconds and reloads the dashboard when Safari or another client writes changes, so Safari Save/Known/Forgot/Ignore updates appear without manual Refresh. The manual Refresh button remains as an explicit recovery control. | `npm run check`, `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-auto-refresh-build npm run build:safari:mac`; manual Safari save auto-refresh walkthrough pending |
| T056 | done | Undo and action feedback | Word actions now capture the previous AppState before mutation, show a footer confirmation, and expose a one-step Undo that restores the prior JSON state. List Save/menu actions and word detail sheet actions all share the same action pipeline, so feedback and Undo behave consistently across entry points. | `npm run check`, `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-undo-build npm run build:safari:mac`; manual action/undo walkthrough pending |
| T057 | pending | Daily review reminders | Add a low-friction reminder surface for due reviews, such as menu bar status, notification permission flow, or in-app badge behavior. | Design first, then compile-only Safari/Mac build |
| T058 | done | Basic/Pro monetization todo | `docs/MONETIZATION_TODO.md` defines the one-month trial, Basic one-time purchase, Pro subscription boundary, current capability map, implementation todo, price-target verification tasks, and explicit "do not build" rules for portable export/import. | Document review |
| T059 | done | Basic entitlement schema and paywall design | `docs/MONETIZATION_TODO.md` defines the local entitlement record, trial start/expiry rules, Basic purchase and receipt status fields, post-trial lock behavior, Basic/Pro paywall copy, Settings entitlement UI, restore purchase behavior, offline behavior, and no-data-loss rules before StoreKit code. | Document review |
| T059a | done | Basic local entitlement skeleton | AppState now has normalized `entitlements`, access computation, default trial metadata, migration from missing/bad entitlement state, and tests for trial/expired/basic/pro/dev override. Mac app initializes missing entitlements, computes local access, and Settings shows current tier, trial days, Basic status, and a development override for Trial/Expired/Basic/Pro states. | `npm run check`, `npm test`, `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-entitlement-build-2 npm run build:safari:mac` |
| T060 | done | StoreKit Basic one-time purchase MVP | Mac Settings drives StoreKit 2: loads the Basic product, shows price, Unlock Basic, and Restore Purchase for `com.japanstudylab.fadingfurigana.basic.macos`. The dashboard reconciles `Transaction.currentEntitlements` at launch and listens to `Transaction.updates`, while Settings runs its own listener to self-heal interrupted purchases. Trial-expired Basic access is now enforced on both sides of the local loop: Mac app Save opens the Basic paywall through the shared word-action chokepoint, while Safari tooltip Save reloads the latest entitlement, blocks expired-trial saves in `WordRepositoryService`, and shows a localized unlock prompt without hiding existing vocabulary or review. | Live StoreKit test purchase from Xcode flips `entitlements.basic.status = purchased` and access tier to `basic` (verified in `app-state-v1.json` + Settings); `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-basic-paywall-build npm run build:safari:mac`; `npm run check`, `npm test`; manual expired-trial Safari tooltip walkthrough still recommended before beta |
| T061 | pending | Pro subscription architecture prep | Extend sync/entitlement design for account-based Pro subscription: server entitlement verification, lapsed subscription behavior, sync gating, and advanced feature flags. | Design doc update; no code required |
| T062 | done | Configurable smart context display | Added `annotation.useSmartContextDisplay`, enabled by default. Titles, navigation/search/result-like contexts, links, and short UI text render as tap-only hints while normal paragraph text keeps ruby. Popup and Mac Settings expose the toggle with zh/en copy, and annotation tests cover default smart behavior plus disabled fallback. | `npm run check`, `npm test`, `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-smart-context-build npm run build:safari:mac` |
| T063 | pending | Annotation visual customization | Add configurable annotation colors with sane defaults. Start with learning-state colors (`new`, `learning`, `known`, `ignored`, `pinned`/always-show), then consider optional overlays for difficulty (`N5`-`N1`) or part of speech. Settings should expose a dedicated customization section/page with reset-to-default and accessibility-safe contrast. | Design first; then `npm run check`, `npm test`, Safari/Mac build, and real-page link/contrast smoke tests |
| T064 | done | Settings panel information architecture redesign | Extension popup is now a current-browser control panel: current-site status, pause/enable site, quick display/level controls, compact today summary, collapsed secondary controls, immediate saves, and no Reset/Save settings flow. App settings is now grouped into Learning Experience, Appearance, Data and Privacy, Extensions, and Purchase and Sync, with Safari shared-local-data and Chrome local-only-until-Pro messaging. AppState adds `settings.siteOverrides` so site pause works across Safari/Chrome. | `npm run check`, `npm test`, `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-settings-ia-build npm run build:safari:mac`; manual Safari popup/App settings walkthrough still recommended |
| T065 | done | Fix content-tab persist wiping Mac app review data | `WordRepositoryService.persistImmediately` overwrote shared state with the tab's stale in-memory copy, re-merging only `settings`/`entitlements`. After a Mac app review, the next Safari word action wiped `reviewLogs` and per-word `learning` schedule. Now persists merge every record-keyed domain (`lexicalItems`, `userLexicalStates`, `sourceOccurrences`, `dailyExposureSummaries`, `reviewLogs`) per-record by `updatedAt` (deviceId tie-break) per `core-schema/merge-rules.md`, so no domain is wiped wholesale. | `npm run check`, `npm test` (124 cases incl. new regression "persist preserves review logs and schedule written elsewhere since load"); `npm run build:safari:mac` + live Safari verification: Mac app review → Safari word action, reviewLogs (11) and all 7 word schedules preserved |
| T066 | done | Evict stale exposure-only word records (storage growth) | Exposure-only word records (`lexicalItems` + `userLexicalStates`) accumulated without bound (`compactState` only expired summaries and stripped meanings). Observed 2026-06-15 QA: ~2492 words = 6.94 MB. Fixed: `compactState` now evicts word records that are exposure-only (`lifecycleStatus` new), never interacted, and last seen older than `retentionDays`; interacted words (saved/known/ignored/forgot/pinned/reviewed/tooltip-opened) are always kept. Added shared `isInteractedState` helper. | `npm run check`, `npm test` (new case "compactState evicts stale exposure-only words and keeps interacted or recent ones") |
| T067 | done | Gate Forgot on unsaved words after trial expiry | Decision 2026-06-15: gate Save + Forgot, keep Known/Ignore. Forgot (`resetLearning`) sets `userIntent.saved = true`, so on an unsaved word it builds the same paid saved-learning asset as Save. `markForgotten` now calls `assertBasicUnlockedForSave` unless the word is already saved (a review re-grade stays available); the Safari tooltip's Forgot handler catches `BasicAccessLockedError` and shows the unlock prompt like Save. Known/Ignore stay open (they only suppress annotation). `ENTITLEMENT_FEATURE_MATRIX.md` already matches. | `npm run check`, `npm test` (new case "blocks forgetting an unsaved word after trial expiry but allows re-forgetting a saved word") |
| T068 | done | "已标记为忘记" toast localization — not a defect | Investigated 2026-06-15: the toast call site already localizes via `L.t("Marked as forgotten.")` (`ViewController.swift:1632`) and the string exists in `Localization.swift`. The Chinese text observed under an English UI was a stale toast generated before the in-session language switch (already-displayed toasts are not re-localized on language change). No code change needed. | n/a — verified in source |
| T069 | done | Storage scaling Phase 1 — exposure light layer + promotion | Exposure-only browsing now writes compact `exposureIndex` records plus `dailyExposureSummaries` instead of materializing full `lexicalItems`/`userLexicalStates`; first interaction promotes indexed `seenCount`/`firstSeenAt`/`lastSeenAt` into a full user state. Existing full exposure-only records are still readable and compaction now runs after merge so stale records are not resurrected from storage. Mac Discover/Library reads the index and Mac actions also promote it. Added same-service monotonic mutation timestamps to avoid same-millisecond LWW self-clobber while keeping T065 merge safety. | `npm run check`; `npm test` (incl. exposure-index write, promotion, T065 review preservation, and compaction-after-merge regression); `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-t069-build npm run build:safari:mac` |
| T070 | done | Storage scaling Phase 2 — single-writer fast path | AppState metadata now carries `storageRevision` + `writeId`; JS adapters, Safari native bridge, and Mac app writes bump them on save. `WordRepositoryService.persistImmediately` compares the currently stored signature with the signature loaded by the service: unchanged storage takes the direct compact+save fast path, while changed storage falls back to the T065 record-domain merge. The signature also includes `updatedAt` so cached old content scripts that only bump timestamps still trigger conflict handling. | `npm run check`; `npm test` (incl. fast-path skip, conflict merge, T065 review-preservation, T069 promotion/compaction regressions); `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-t070-build npm run build:safari:mac` |
| T071a | done | Storage scaling Phase 3 prep — record model + migration validation | Added the sync-shaped record-store snapshot v1 contract without switching runtime storage: deterministic record IDs, singleton records for metadata/profile/settings/entitlements, per-domain records for lexical/user/exposure/source/daily/review, AppState v1 → record snapshot migration, snapshot → AppState reconstruction, validator, and a small per-record merge prototype for conflict fixtures. Exposure additive deltas remain explicitly pending until the sync protocol is finalized. | `node packages/core-schema/tests/recordStoreMigration.test.js` (schema/type coverage, round-trip, fixed-input idempotence, tombstone preservation, large exposure-heavy fixture, append-only/conflict merge, invalid id + dangling reference validation); then covered by `npm run check` / `npm test` |
| T071 | pending | Storage scaling Phase 3 — per-record storage (with sync) | Move off the monolithic blob: per-key entries on browser `storage.local`; SQLite (or sharded files) in the App Group on macOS, Swift-owned. Per-save cost becomes O(change); foundation for the Pro sync per-record op-log. Land as a standalone, golden-vector-tested local migration BEFORE wiring sync; do not bundle with the sync rollout. Honor all migration-safety constraints in `docs/STORAGE_SCALING_DESIGN.md` §8 (versioned forward-only, pre-migration backup, downgrade guard, extension↔app format agreement across Safari's cached content scripts). Do during beta while the base is small. | TBD with the sync epic |
| T072 | done | Lock Extension/App decoupling target architecture | Rewrite `docs/EXTENSION_APP_DECOUPLING_DESIGN.md` around independently shippable Mac Safari extension container, Mac Chrome extension, Mac App, iOS Safari extension container, iOS App, and non-target iOS Chrome extension; add `docs/INGEST_PROTOCOL.md` defining recordBatch/ack/cursor/merge/transport requirements. | Document review; no runtime code changed |
| T073 | done | JS ingest core and golden vectors | Added `createRecordBatch`, `validateRecordBatch`, `mergeRecord`, and `ingestBatch` in `packages/core-schema/src/recordStore.js`, using `recordId` as the merge key. New ingest tests cover replay idempotence, same logicalId across record types, LWW device tie-break, append-only coexistence, deterministic ID rejection, local-only preservation, and pending additive strategy warnings. | `node packages/core-schema/tests/recordStoreMigration.test.js`; `node packages/core-schema/tests/ingestProtocol.test.js`; `npm run check`; `npm test` |
| T074 | done | Extension standalone data boundary | Kept extension annotation/exposure independent from App learning assets: locked Save/Forgot copy now says saving is a Learning App Basic feature, popup access copy says annotation/exposure remain local/free, `BasicAccessLockedError` now names saved learning assets, and tests lock that passive exposure still records after trial expiry. Marked the Safari `loadState/saveState` bridge as legacy whole-state transport. | `node packages/extension/tests/tooltip.test.js`; `node packages/extension/tests/wordRepositoryService.test.js`; `npm run check`; `npm test` |
| T075 | done | Mac App ingest hub over local records | Added a Swift-side `recordBatch` ingest entry point and record merge implementation over the existing local `app-state-v1.json` domains. The Mac App can now accept normalized records through `AppStateStore.ingestRecordBatch` without assuming the records came from a bundled Safari extension, while preserving the legacy monolithic AppState file during migration. | `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-t075-build npm run build:safari:mac` |
| T075a | done | Swift ingest golden-vector harness | Added shared ingest golden vectors and a Swift command-line harness that compiles the same `IngestProtocol.swift` used by the app, then verifies JS and Swift agree on AppState projection for same-logicalId records, append/LWW tie-breaks, invalid deterministic IDs, and local-only metadata. Also fixed Swift ISO timestamp parsing for fractional seconds. | `node packages/core-schema/tests/ingestProtocol.test.js`; `npm run test:swift-ingest`; compile-only Mac/Safari build |
| T076 | in_progress | Safari App Group recordBatch bridge | Code path wired: native Safari handler supports `ingestRecordBatch` and `pullRecordBatch` beside legacy `loadState/saveState`; JS `SafariNativeStorageAdapter.loadState()` now prefers `pullRecordBatch`; `saveState()` prepares AppState, pushes a recordBatch, then pulls the native batch back; older native handlers still fall back to whole-state actions. Shared Swift `IngestProtocol.swift` compiles into app and extension targets. Signed local install succeeded; remaining work is a real Safari page round trip after Safari reloads the latest extension scripts. | `npm run check`; `npm test`; `npm run test:swift-ingest`; `FURI_SAFARI_COMPILE_ONLY=1 FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-t076-runtime-build npm run build:safari:mac`; `FURI_DEVELOPMENT_TEAM=W3JAJZ2RRU FURI_XCODE_DERIVED_DATA=/tmp/fading-furigana-signed-t076 npm run build:safari:mac`; page round trip pending |
| T077 | in_progress | Mac Chrome loopback bridge | Bridge skeleton (T077 phase 1): browser-side shared recordBatch codec, Chrome loopback client with port discovery (`127.0.0.1:57310-57330`), durable pending-batch queue, buffer-and-flush, pull support; macOS-only `LoopbackIngestServer` with `/health`, `/ingest-record-batch`, `/pull-record-batch`, 1 MB cap, CORS echo, bearer hook, `network.server` entitlement. **Pairing UX phase added:** the pairing code now doubles as the bearer token — `LoopbackPairing` generates/persists an 8-char Crockford code on first launch (port never unauthenticated), normalizes both sides (`A-Z0-9`), and records listening port + last-connected status; the macOS Settings › Extensions pane shows the code with Copy/New Code, server status, and last-connected; the Chrome popup gained a Mac-app panel (`getLoopbackClient`/`setToken`/`verifyPairing`) that pairs, distinguishes no-server vs rejected-code, then syncs. Routing was extracted into a transport-independent `LoopbackRouter`/`LoopbackResponse`, and a real-socket smoke harness (`test:loopback-smoke`) drives the production parser/router/auth/IngestProtocol over real 127.0.0.1 TCP (NWListener itself can't bind in headless CI, but raw sockets can). The Origin 403 was dropped in favor of token-only auth (web pages can neither write without the code nor read without the chrome-extension ACAO echo). **Bind bug found by running the real app and fixed:** `NWListener` was created with both `requiredLocalEndpoint` (loopback-only) and `on:` port, which is rejected with EINVAL, so the server never bound (zero network fds) — and port conflicts surface asynchronously, not as throws, so the old synchronous fallback loop couldn't recover. Rewrote `start()` to use `requiredLocalEndpoint` only and walk the port range on the server queue via the state handler. Verified against the running signed sandboxed app: lsof shows it listening on 127.0.0.1:57310, `/health` returns `pairing:token`, unauthenticated/wrong-code ingest → 401, authorized ingest with the app's real code applied both records (storageRevision bumped), pull returns them, and `LoopbackPairing` status (`chrome-extension`, port 57310) persisted to the container prefs. **Background-worker routing added:** the background service worker now owns the one `MacLoopbackClient`; `ChromeStorageAdapter` and the popup talk to it through a `BackgroundLoopbackClient` proxy (direct client kept as a fallback when there's no messaging), so loopback fetches are first-party extension requests instead of content-script fetches carrying a page Origin (which the server applied but the page could not read, leaving the queue stuck). Remaining: manual Chrome-browser GUI click-through. | `npm run check`; `npm test` (160 pass); `npm run test:swift-ingest`; `npm run test:loopback-smoke`; signed `FURI_DEVELOPMENT_TEAM=W3JAJZ2RRU npm run build:safari:mac` → BUILD SUCCEEDED + installed; real-app loopback round trip via curl (health/401/ingest/pull) verified |
| T078 | pending | Apple target/listing split | Split Safari extension container from the learning App after ingest bridge is stable: thin Mac/iOS Safari container apps carry appex; Mac/iOS learning apps no longer require Safari extension targets; update build scripts so dist resources feed the thin containers. | Xcode build list; `npm run package:safari`; compile-only builds for new targets; App Group entitlement check |
| T079 | pending | Monetization and entitlement docs for decoupled products | Update `ENTITLEMENT_FEATURE_MATRIX.md`, `MONETIZATION_TODO.md`, `APP_STORE_METADATA_DRAFT.md`, support/privacy copy, and `CLAUDE.md` hard rule #4 so free annotation/exposure, paid local learning, and Pro cloud sync are described consistently. | Document review |

## Update Rules

- When starting a task, change status to `in_progress`.
- When finishing a task, change status to `done` and add verification notes if needed.
- If a task cannot continue without external input, change status to `blocked` and describe the blocker.
- Keep tasks small enough to complete in one focused development session.

## Last Updated

```text
2026-06-18
```
